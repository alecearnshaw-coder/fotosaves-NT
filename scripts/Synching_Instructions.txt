NO NEED TO RUN SYNC WHEN THE DATABASE FILES ARE UUPDATED
The JSON Database files now only reside in /public/data. Exclusive "source of truth"
The JSON database files in /src/data no longer exist. Removed 9th August 2026.

How to Run It Yourself
Option 1: From Cursor's terminal (press Ctrl+` to open)
powershell
.\scripts\sync-data.ps1

Or via npm:
npm run sync

Your Site: Hybrid Architecture
You're right - it's not a simple static site. It's a dynamic site with client-side rendering:
800+ species pages generated dynamically from JSON data
Group pages (orders, families, subfamilies) built on-the-fly
Currently rendered in the browser (CSR)

Current Workflow
┌─────────────────┐      fetch      ┌─────────────┐
│  public/data/   │ ◄─────────────  │  Browser    │
│  (single copy)  │                 │  (renders)  │
└─────────────────┘                 └─────────────┘
         ▲
         │ read (fs)
┌─────────────────┐
│  Next.js server │
│  (SSR / APIs)   │
└─────────────────┘

Edit JSON files directly in public/data/. No data sync step.

However, is making any edits fo builder HTML/JS/CSS in src/builders/, run:
.\scripts\sync-data.ps1  which will synch some folders.
Then commit and push the public/ changes.

Summary
Folder	Purpose	Edit?
public/data/	Single source of truth for taxonomy/species JSON	Yes
src/builders/	Builder logic (synced to public for CSR)	Yes
public/*.html	Currently served HTML files (synced from builders)	Via sync
